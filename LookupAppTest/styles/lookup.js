import { StyleSheet } from 'react-native';

const lookupStyles = StyleSheet.create({
    label : {
        fontSize: 20,
        fontWeight: '700'
    },
    inputs : {
        borderWidth: 3,
        borderColor: '#ccc',
        borderRadius: 10       
    }
});

export default lookupStyles;